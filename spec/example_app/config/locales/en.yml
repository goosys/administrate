en:
  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  helpers:
    label:
      order:
        address: Address
        details: Details

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Administrate-prototype

  activerecord:
    models:
      customer: Customer
      order: Orders
      page: page
      product: products
      log_entry: Log Entry
      product_meta_tag: Product Meta Tags
      blog_post: blog post
      blog_tag: blog tags
      series: _series
      stats: _stats